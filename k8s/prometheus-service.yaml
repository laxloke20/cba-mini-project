apiVersion: v1
kind: Service
metadata:
  name: cba-prometheus-service
spec:
  selector:
    app: cba-prometheus
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
      nodePort: 30090
  type: NodePort