apiVersion: v1
kind: Service
metadata:
  name: cba-grafana-service
spec:
  selector:
    app: cba-grafana
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30300
  type: NodePort